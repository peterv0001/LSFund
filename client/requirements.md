## Packages
framer-motion | Page transitions and complex animations for the binary tree
recharts | Visualizing commission stats and growth trends
lucide-react | High quality icons for the UI (already in base but explicit check)
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
Binary tree visualization needs to handle recursive data structures from /api/agents/:id/team.
Auth flow uses cookie-based sessions (credentials: "include").
Dashboard needs real-time feel for stats.
Colors should be deep navy blue (trust) and gold/emerald (wealth).
